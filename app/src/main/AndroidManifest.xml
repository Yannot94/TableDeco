<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="android.yannot.fr.tabledeco">
<!--
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"></application>
    -->
<uses-sdk android:minSdkVersion="14" />

<application
    android:icon="@drawable/ic_launcher"
    android:label="@string/app_name" >
    <activity
        android:label="@string/app_name"
        android:name="fr.yannot.android.tabledeco.MaDecoTablesActivity" android:theme="@style/apfTheme">
        <intent-filter >
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
    <activity android:theme="@style/apfTheme" android:name="fr.yannot.android.tabledeco.ViewTableActivity"></activity>
    <activity android:theme="@style/apfTheme" android:name="fr.yannot.android.tabledeco.PlongeeSimpleActivity"></activity>
    <activity android:theme="@style/apfTheme" android:name="fr.yannot.android.tabledeco.PlongeeSuccessiveActivity"></activity>

</application>

</manifest>